import React from 'react';
import DefaultIcon from 'react-native-vector-icons/Ionicons';

const Ionicons = ({name, size, color}) => {
  return <DefaultIcon name={name} size={size} color={color} />;
};

export default Ionicons;

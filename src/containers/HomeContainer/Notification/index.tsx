import React from 'react';
import {AppBar} from 'components';

type Props = {};

const Notification: React.FC<Props> = ({}) => {
  return <AppBar title="Notifikasi" />;
};

export default Notification;

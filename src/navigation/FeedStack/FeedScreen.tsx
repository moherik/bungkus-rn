import React from 'react';
import {FeedContainer} from 'containers';
import {FeedScreenProps} from './index';

const FeedScreen = ({}: FeedScreenProps) => <FeedContainer />;

export default FeedScreen;
